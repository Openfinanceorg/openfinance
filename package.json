{
  "name": "openfinance-app",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "docker compose up -d && concurrently \"pnpm --filter server dev\" \"pnpm --filter client dev\" \"pnpm --filter client storybook\"",
    "dev:server": "docker compose up -d && pnpm --filter server dev",
    "build": "docker build -t openfinance-api .",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.7.0"
  }
}
